services:services:$schema: "http://json-schema.org/draft-07/schema#"

  - type: web

    name: spotter-express-api  - type: webservices:

    env: node

    region: oregon    name: spotter-express-api  - type: web

    plan: free

    buildCommand: npm install && npm run build    env: node    name: spotter-express-api

    startCommand: npm start

    envVars:    region: oregon    env: node

      - key: NODE_ENV

        value: production    plan: free    region: oregon

      - key: FRONTEND_URL

        sync: false    buildCommand: npm install && npm run build    plan: free

    healthCheckPath: /health

    startCommand: npm start    buildCommand: npm install && npm run build

    envVars:    startCommand: npm start

      - key: NODE_ENV    envVars:

        value: production      - key: NODE_ENV

      - key: FRONTEND_URL        value: production

        sync: false      - key: FRONTEND_URL

    healthCheckPath: /health        sync: false

    healthCheckPath: /health
